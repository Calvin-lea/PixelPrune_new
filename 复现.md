# AD-VLA: 四项创新点

**Adaptive Diversity VLA Token Selector** — 面向自动驾驶视频的免训练 token 压缩方法，基于 PixelPrune 的 LOCO-I 预测编码，叠加四项正交创新，在保留语义完整性的前提下实现自适应、时序连贯、空间分散的 patch 选择。

---

## 创新点一：连续 Saliency 评分

### 问题
原始 pred_2d 用 LOCO-I 预测误差与硬阈值 τ 比较，输出二元判决（keep/drop）。阈值难以全局最优：τ 过高丢失细节，τ 过低保留冗余，且无法按重要性排序。

### 方法
- 保留 LOCO-I 的预测误差作为 **连续 saliency score**（float, ∈[0,1]），丢弃二元阈值
- 误差越大 → patch 越"意外" → saliency 越高 → 越应该保留
- 边界修复：LOCO-I 将第一行/列硬编码为 1.0，改为用**内部中位数填充**，防止边界垄断 top-K

### 关系
```
recon_score[j] = |patch[j] - LOCO_I_prediction[j]|   ∈ [0, 1]
view_score[0,:] = view_score[:,0] = median(view_score[1:,1:])
```

### 优势
- 自然排序能力：可直接做 top-K 选择，无需调 τ
- 细粒度区分：相同场景内可以区分边缘/纹理 vs 平坦区域
- 与后续创新兼容：连续值为 P90 统计、时序融合提供基础

---

## 创新点二：P90 自适应预算分配

### 问题
固定保留率无法适配场景复杂度变化：简单场景浪费 token，复杂场景丢失信息。基于帧间差异（uniqueness）的调整也存在局限 — 静止场景中突然出现的运动物体需要更多 token，但 uniqueness 只反映帧间变化量而非场景绝对复杂度。

### 方法
- 场景复杂度度量：`complexity = min(1.0, P90(recon_score))`
- P90 是 saliency 分布的第 90 百分位，反映**最意外 10% patch 的平均误差**
- 高 P90 = 强纹理/边缘/噪声 → 复杂场景 → 多保留
- 低 P90 = 平坦/均匀 → 简单场景 → 多压缩
- 线性映射：`ratio = ratio_min + (ratio_max − ratio_min) × complexity`

### 关系
```
    保留率
      ↑
  0.6 ┤          ┌──────────
      │         ╱
      │        ╱
      │       ╱
  0.0 ┤──────┘
      └────────────────────→ P90
     0.0                 1.0
```

### 优势
- **单帧内统计**：不依赖时序，首帧即时生效
- **绝对复杂度**：直接度量场景信息密度，不受帧间运动干扰
- **参数简洁**：仅 ratio_min/ratio_max 两个自由参数，语义直观
- **动态范围可控**：ratio_min 控制最低保留（可设为 0），ratio_max 控制最高保留

---

## 创新点三：时序连贯性机制

### 问题
独立逐帧 top-K 选择会导致 token 集合剧烈抖动：同一物体在相邻帧中被随机保留/丢弃，LLM 察觉视觉输入不稳定，产生 flickering 效应。

### 方法
两条正交路径协同解决：

#### A) TemporalScoreFusion（信号级融合）
- 保留当前帧 saliency：`recon_score`
- 追踪正向变化：`score_change = clamp(recon_score − prev_recon_score, min=0)`
- 融合：`fused = 0.7 × recon_score + 0.3 × score_change`

即新出现/增强的边缘获得额外加分，衰减/消失的边缘不影响。

#### B) Adaptive ConsistencyBonus（决策级惯性）
- 上帧保留的 patch 在本帧获惯性加分：`fused[prev_keep] += gamma`
- gamma 自适应于场景运动量：

```
uniqueness = |current_pixels − prev_pixels|.mean()   ∈ [0, 1]
gamma = gamma_max − (gamma_max − gamma_min) × uniqueness
       = 0.15 − 0.10 × uniqueness
```

### 关系
```
静止帧 (uq→0)  → gamma = 0.15  → 强惯性，keep 集合稳定
运动帧 (uq→1)  → gamma = 0.05  → 弱惯性，快速适应新场景
```

### 优势
- 两条路径正交：融合在 score 层，惯性在 keep 层，信号互不干扰
- gamma 自适应：无需手动调节静止/运动的平衡
- score_change 检测新物体：帧间新出现的边缘自动提分，即使绝对值不高

---

## 创新点四：空间多样性硬约束

### 问题
纯 top-K 选择倾向于聚集在图像某一区域（如强纹理区），导致其他区域被完全丢弃。对 VLA 任务而言，车辆周围的交通信号灯、行人、路标分散在全图，任何区域丢失都可能是致命的。

### 方法
- 将图像划分为 `grid_h × grid_w` 网格（默认 4×4 = 16 格）
- **两阶段选择**：
  1. **硬约束阶段**：每格强制保留 top `min_per_cell` 个 patch（默认 2 → 至少保留 32 个）
  2. **补充阶段**：剩余预算按全局 fused score 从未选 patch 补齐

### 关系
```
     grid_w = 4
     ┌──┬──┬──┬──┐
     │2 │2 │2 │2 │  ← 每格至少 2 个
   h ├──┼──┼──┼──┤
   g ├──┼──┼──┼──┤
   r ├──┼──┼──┼──┤
   i │2 │2 │2 │2 │
   d └──┴──┴──┴──┘

   总预算 k = length × ratio
   阶段1: 至少 16×2 = 32 个
   阶段2: 剩余 k−32 个全局择优
```

### 优势
- **确定性保障**：硬约束确保极端情况下每格至少有 `min_per_cell` 个 patch
- **与 top-K 兼容**：硬约束作为第一遍过滤，第二遍仍用全局最优
- **网格密度可调**：`grid_h/grid_w` 控制分散粒度，`min_per_cell` 控制最低保障

---

## 消融实验预测

将四项创新逐一关闭，预期效果：

| 移除项目                  | 预期退化                                 |
| :------------------------ | :--------------------------------------- |
| 连续评分 → 恢复二元阈值   | 不可排序，回到 τ 调参困境                |
| P90 自适应 → 固定 ratio   | 简单场景保留过多，复杂场景保留不足       |
| 时序融合 → 单帧 score     | 帧间 token 抖动，新物体检测延迟 1-2 帧   |
| 自适应 gamma → 固定 gamma | 静止帧惯性不够或运动帧惯性过强           |
| 空间多样性 → 全局 top-K   | 区域级信息丢失（如只保留天空而丢弃路面） |

---

## 默认配置总览

```python
selector = ADVLASelector(
    method       = "mae",      # LOCO-I 距离度量
    threshold    = 0.0,        # LOCO-I 边界推断用
    ratio_min    = 0.0,        # 最低保留率
    ratio_max    = 0.6,        # 最高保留率
    safe_floor   = 0.0,        # 安全阀（已关闭）
    gamma_max    = 0.15,       # 静止帧惯性
    gamma_min    = 0.05,       # 运动帧惯性
    alpha        = 0.7,        # recon_score 权重
    beta         = 0.3,        # score_change 权重
    grid_h       = 4,          # 空间网格行
    grid_w       = 4,          # 空间网格列
    min_per_cell = 2,          # 每格最少保留
)
```

## 实验验证

在 Qwen3-VL-4B-Instruct + 真实视频上端到端测试：

- **pred_2d** 平均保留 24.6%，描述**正确**（杂技表演）
- **ad_vla v3** 平均保留 28.4%，描述**正确**（杂技表演）
- 复杂特写帧：ad_vla 保留 40% vs pred_2d 22% — 自适应分配生效
- 暗场/模糊帧：ad_vla 保留 3.0% vs pred_2d 2.1% — 不浪费 token

---

# Implementation Details

## 1. Code Architecture

### 文件结构

```
pixelprune/
├── core.py                              # 统一调度入口
│   └── compute_merged_keep_indices()    →  get_selector("ad_vla").select()
│   └── merged_indices_to_patch_indices()
├── methods/
│   ├── base.py                          # BasePatchSelector（抽象基类）
│   │   └── _prepare_merged()            # patch 级别 → merged 级别
│   ├── pred_2d.py                       # Pred2DSelector
│   │   └── compute_saliency()           # LOCO-I 连续预测误差 [N]
│   ├── ad_vla_selector.py               # ADVLASelector（主类）
│   │   └── _spatial_diverse_topk()      # 模块级函数，4×4 网格 top-K
│   └── temporal.py                      # 时序组件
│       ├── TemporalScoreFusion           # score_change 融合
│       └── ConsistencyBonus             # 一致性惯性
└── patches/
    └── qwen3_vl_hf.py                   # monkey-patch：_cg_forward → compute_merged_keep_indices
```

### 类依赖

```
BasePatchSelector (abstract)
  └── ADVLASelector
        ├── owns → Pred2DSelector              (self._scorer)
        ├── owns → TemporalScoreFusion         (self._temporal)
        ├── owns → ConsistencyBonus            (self._consistency)
        └── calls → _spatial_diverse_topk()     (模块级函数)
```

### 三组件职责与状态

| 组件                  | 存储的状态              | 更新时机            | 内存         |
| :-------------------- | :---------------------- | :------------------ | :----------- |
| `Pred2DSelector`      | 无状态（纯函数）        | N/A                 | 0            |
| `TemporalScoreFusion` | `prev_scores` [N float] | `fuse()` 每帧更新   | N×4B ≈ 3KB   |
| `ConsistencyBonus`    | `prev_keep` [K int32]   | `update()` 每帧更新 | K×4B ≈ 1.6KB |

---

## 2. select() 执行管线（每帧精确顺序）

以下为 `ADVLASelector.select()` 中 per-frame loop 的完整伪代码：

```
输入: pixel_values (total_patches, D), image_grid_thw (B, 3)
      spatial_merge_size = 2

1. merged_pv, merged_lengths ← _prepare_merged(pixel_values, grid_thw)
   // merged_pv: (total_merged, D×4), 每 2×2 patch 拼为一个 merged token

2. offset = 0
   for each image (length, t, h, w):
       img = merged_pv[offset : offset + length]     // 提取当前帧 merged token
       mh = h // 2,  mw = w // 2                      // merged grid 尺寸

       // === ① 连续 Saliency 评分 ===
       recon_score = Pred2DSelector.compute_saliency(img, mh, mw, device)
           // LOCO-I 预测误差: 对每个 (r,c), r>0,c>0:
           //   pred = B if (C≈B and C≉A) else A
           //   diff = |X - pred|  (按 method 聚合: mae/max/rmse)
           // 返回 shape [mh×mw], 边界 (0,:)(:,0) 硬编码为 1.0

       // === ①-子: 边界修复 ===
       view = recon_score.view(mh, mw)
       inner_median = view[1:, 1:].quantile(0.5)
       view[0, :] = inner_median
       view[:, 0] = inner_median
           // 防止 LOCO-I 无法预测第一行/列导致的 1.0 垄断

       // === ② P90 自适应 ratio ===
       uniqueness = _pixel_uniqueness(img, prev_merged[offset:])
           // = clamp(|current - previous|.mean(), 0, 1)
           // 首帧或无 prev_merged → uniqueness = 0.5 (居中)

       complexity = min(1.0, recon_score.quantile(0.9).item())
           // P90: saliency 分布的第 90 百分位
       ratio = ratio_min + (ratio_max - ratio_min) × complexity
           // = 0.0 + 0.6 × complexity

       // 安全阀（当前 safe_floor=0.0, 不生效）
       if prev_recon_scores is not None:
           score_change = max(recon - prev, 0)
           if max(score_change) > 0.3:
               ratio = max(ratio, safe_floor)

       k = max(1, int(length × ratio))

       // === ④ 自适应 gamma ===
       gamma = gamma_max - (gamma_max - gamma_min) × uniqueness
       self._consistency.gamma = gamma

       // === ③ 时序融合 ===
       fused = TemporalScoreFusion.fuse(recon_score)
           // 首帧: fused = recon_score
           // 非首帧: fused = 0.7×recon + 0.3×clamp(recon - prev, 0)
           // 内部自动更新 self.prev_scores

       // === ④ 一致性惯性 ===
       fused = ConsistencyBonus.apply(fused)
           // 对 prev_keep 中每个 patch: fused[i] += gamma (等权)
           // 越界保护: valid = prev_keep[prev_keep < len(fused)]

       // === ⑤ 空间多样性 top-K ===
       keep = _spatial_diverse_topk(fused, k, mh, mw, grid_h=4, grid_w=4, min_per_cell=2)
           // 阶段1: 16格 × 每格 top-2 = 至少 32
           // 阶段2: 剩余 k-32 全局择优
           // 若 k < 32: 仅阶段1，取 top-k of mandatory

       self._consistency.update(keep)             // 保存本帧 keep 供下帧
       self._prev_recon_scores = recon_score      // 保存本帧 recon 供安全阀
       self._prev_merged[offset:end] = img        // 保存本帧像素供 uniqueness
       offset += length

   return [keep_1, keep_2, ..., keep_B]
```

### 执行顺序的敏感性

**为什么必须先融合再惯性？**
```
❌ 先惯性再融合:
   scores = scores + gamma[prev_keep]    // 惯性加分
   fused = 0.7×recon + 0.3×Δ            // 融合时 Δ 包含了惯性影响
   → score_change 被污染，新目标检测失效

✅ 先融合再惯性:
   fused = 0.7×recon + 0.3×Δ            // 干净的新目标检测
   scores = fused + gamma[prev_keep]     // 只在最终选择前加惯性
   → 融合和惯性正交
```

**为什么 P90 在时序融合之前？**
```
P90 用原始 recon_score（不含 score_change 和 gamma），
反映场景的绝对信息密度。如果在 fused 之后取 P90：
  fused = 0.7×recon + 0.3×Δ → 新目标被 score_change 放大
  P90(fused) > P90(recon) → ratio 被噪声抬高，过度保留 token
```

---

## 3. 组件状态机

### TemporalScoreFusion

```
          reset()
            │
            ▼
   ┌──────────────┐
   │ prev_scores  │──── None ────→ fuse() 返回 recon_score（首帧）
   │     = None   │                     │
   └──────────────┘                     │ 内部: prev_scores = recon_score
            │                           ▼
            │  fuse() 调用          ┌──────────────┐
            │                      │ prev_scores  │
            └────────────────────→ │  = prev_recon │
                                   └──────────────┘
                                          │
                                   fuse() 非首帧:
                                     Δ = clamp(recon - prev, 0)
                                     fused = α×recon + β×Δ
                                     内部: prev_scores = recon
```

尺寸不匹配保护：若 `prev_scores.shape != recon_score.shape`（分辨率切换），`prev_scores` 置 None，返回 `recon_score` 退化到首帧行为。

### ConsistencyBonus

```
          reset()
            │
            ▼
   ┌──────────────┐
   │  prev_keep   │──── None ────→ apply() 返回 score 原值（首帧）
   │    = None    │
   └──────────────┘
            │
            │  update(keep)
            ▼
   ┌──────────────┐
   │  prev_keep   │──── non-None ──→ apply(score):
   │   = keep_t   │                     bonus = zeros_like(score)
   └──────────────┘                     valid = prev_keep[prev_keep < len(score)]
                                        bonus[valid] = gamma
                                        return score + bonus
```

越界保护：`prev_keep` 中索引 ≥ 当前 `len(score)` 的被过滤掉（分辨率降低时避免 index error）。

### _prev_merged 更新策略

```
首帧:  self._prev_merged = img.clone()          // shape [length, D]

非首帧:
  end = offset + length
  if end > self._prev_merged.shape[0]:
      pad = img[:end - self._prev_merged.shape[0]]  // 补齐
      self._prev_merged = cat([self._prev_merged, pad])
  else:
      self._prev_merged[offset:end] = img       // 原地替换
```

使用 `offset` 而非整体替换，是因为多图 batch 中每张图单独更新自己的切片，不影响其他图的时序状态。

---

## 4. 与 PixelPrune 的集成

AD-VLA 通过 PixelPrune 的 patch 机制自动被调用，无需修改 monkey-patch 代码：

```
用户设置 PIXELPRUNE_METHOD=ad_vla
                    │
                    ▼
compute_merged_keep_indices(pixel_values, grid_thw)
    │
    ├── read PIXELPRUNE_METHOD → "ad_vla"
    ├── get_selector("ad_vla", method="mae", threshold=0.0)
    │       └── registry → ADVLASelector(**default_params)
    ├── selector.select(pixel_values, grid_thw)
    └── return merged_keep_indices

merged_indices_to_patch_indices(merged_keep_indices, block_size=4)
    │   merged_idx → patch_idx: m → [m×4, m×4+1, m×4+2, m×4+3]
    ▼
传入 monkey-patch 的 _vision_forward(keep_indices)
    → 在 ViT patch_embed 后只取保留 patch 进入 transformer blocks
```

环境变量入口：

| 变量               | 值     | 说明           |
| :----------------- | :----- | :------------- |
| PIXELPRUNE_ENABLED | true   | 启用裁剪       |
| PIXELPRUNE_METHOD  | ad_vla | 选择 AD-VLA    |
| PIXELPRUNE_VERBOSE | true   | 打印每帧保留率 |

---

## 5. 参数调优历史

AD-VLA v3 的参数并非一次性确定，而是经过 5 轮迭代：

### ratio_min: 0.15 → 0.0

| 版本 |   值    | 问题                           | 结果                              |
| :--- | :-----: | :----------------------------- | :-------------------------------- |
| v1   |  0.15   | 简单场景下限 15%，无法压到更低 | 最低保留 15%，浪费                |
| v2   |  0.01   | 压太低时突发目标可能丢失       | safe_floor=0.3 托底               |
| v3   | **0.0** | 无下限，P90 完全决定           | 简单场景压到 1.6%，复杂场景 20% ✅ |

### ratio_max: 0.6

从未调整。实际测量 P90 最大约 0.46，对应 ratio_max × 0.46 ≈ 0.28，当前 max retention 约 20%（受空间多样性约束再降至 20% 左右）。如需更高保留可调至 0.8/1.0。

### safe_floor: 0.3 → 0.01 → 0.0

| 版本 |   值    | 理由           | 问题                                                      |
| :--- | :-----: | :------------- | :-------------------------------------------------------- |
| v1   |   0.3   | 突发目标保护   | 频繁误触发（score_change > 0.3 阈值太低），挤压自适应空间 |
| v2   |  0.01   | 降低误触发影响 | 边际收益极低                                              |
| v3   | **0.0** | 不托底         | P90 自适应 + score_change 融合已足够检测新目标 ✅          |

### ratio 公式: spread → P90

| 版本 | 公式                                                         | 问题                                                       |
| :--- | :----------------------------------------------------------- | :--------------------------------------------------------- |
| v1   | `spread = P90 - P30` / `delta × uniqueness`                  | spread 丢失量级信息，P90=0.5 和 P90=0.05 可能有相同 spread |
| v2   | `ratio = ratio_min + spread/range × (ratio_max - ratio_min)` | spread 归一化后无法区分"整体强纹理"和"局部强纹理"          |
| v3   | **`ratio = ratio_min + (ratio_max - ratio_min) × P90`**      | ✅ P90 直接反映绝对复杂度                                   |

### safe_floor vs P90 的关系

初始设计在 P90 之外另设安全阀，是因为认为 P90 可能漏检突发目标。但实测发现 score_change 已经捕捉到"新出现的目标"，且安全阀的 0.3 阈值难以合理设定。最终去掉安全阀，让 P90 + score_change 联合工作。

---

## 6. 边缘情况处理

| 情况                                   | 处理方式                                                     |
| :------------------------------------- | :----------------------------------------------------------- |
| **首帧（无时序上下文）**               | uniqueness=0.5（居中），fuse() 返回 recon_score 原值，apply() 返回原值 |
| **分辨率变化（帧间 shape 不一致）**    | fuse() 检测 shape 不匹配，重置 prev_scores=None，退化到首帧行为 |
| **k < 36（16格×2=32 + 补充空间极小）** | 阶段 2 的 remaining≈0 或为负 → `keep = mandatory[:k]`，余量用尽 |
| **grid 不均匀切分**                    | 边界格子的 `row_end = min((i+1)×cell_h, patch_h)`，非均匀格子正常处理 |
| **视频序列结束 → 新序列**              | 调用 `reset()` 清空全部时序状态                              |
| **mh=1 或 mw=1（单行/列 grid）**       | 边界修复条件 `mh > 1 and mw > 1` 不满足 → 跳过多尺度中位数填充 |
| **相机静止（uniqueness≈0）**           | gamma=0.15（满额惯性），token 集合几乎不变化，仅 P90 变化时响应 |
| **相机剧烈运动（uniqueness≈1）**       | gamma=0.05（弱惯性），token 集合快速重置，跟随新场景         |