```
在 pixelprune/methods/ 下新建 ad_vla_selector.py，
继承 BasePatchSelector，类定义如下：
class ADVLASelector(BasePatchSelector):
    name = "ad_vla"
    aliases = ["advla", "ad_vla_selector"]
    注意：
    - BasePatchSelector.__init__ 接受 method, threshold, **kwargs
    - 必须在 __init__ 最后调用 super().__init__(method, threshold, **kwargs)
    - 在 pixelprune/methods/__init__.py 中加 register_method("ad_vla", ADVLASelector)
    - select() 方法签名固定为 (self, pixel_values, image_grid_thw, spatial_merge_size=2)
    - select() 返回 List[torch.Tensor]，每个 Tensor 是 1D 且排序的 merged token 索引
    - 首帧无时序上下文，uniqueness 硬编码为 0.5
    - 多图 batch 中每张图的 merged token 用 offset 定位，update _prev_merged 时只改对应切片
```
## 时序组件
```
在 pixelprune/methods/temporal.py 中实现两个类：
class TemporalScoreFusion:
    - 存储：self.prev_scores (Tensor | None, shape [N])
    - fuse(recon_score) → Tensor:
        首帧直接返回 recon_score
        非首帧：Δ = clamp(recon - prev, 0), fused = α×recon + β×Δ
        内部自动更新 prev_scores = recon_score.detach().clone()
        形状不匹配时重置 prev_scores=None
    - reset(): prev_scores = None
class ConsistencyBonus:
    - 存储：self.prev_keep (Tensor | None, shape [K]), self.gamma (float)
    - apply(scores) → Tensor:
        首帧直接返回 scores
        非首帧：bonus = zeros(N), bonus[prev_keep] = gamma, return scores + bonus
        注：prev_keep 中越界索引过滤：valid = prev_keep[prev_keep < len(scores)]
    - update(keep_indices): prev_keep = keep_indices.detach().clone()
    - reset(): prev_keep = None
```
## 空间多样性 top-K
```
这是一个模块级函数（非类方法），放在 ad_vla_selector.py 顶部。
def _spatial_diverse_topk(scores, k, patch_h, patch_w, grid_h=4, grid_w=4, min_per_cell=2) → Tensor:
    - 阶段1：16格 × 每格选 top-min_per_cell（硬约束）
    - 阶段2：剩余 k-已选 全局补足
    - 当 k < 32（16×2）时：返回 mandatory[:k]
    - 边界格子处理：min(cell_end, patch_h/w)
    - 返回已排序索引，shape ≤ k
```
## select() 内的执行顺序
```
在 per-frame loop 中，顺序必须严格如下：
for each frame:
    1. compute_saliency(img, mh, mw)                    # Pred2DSelector
    2. 边界修复：第一行/列 ← 内部中位数
    3. P90 → complexity → ratio → k                      # P90 取自修复后的 recon_score
    4. uniqueness → gamma                                # 自适应 gamma
    5. fused = temporal.fuse(recon_score)                # 时序融合
    6. fused = consistency.apply(fused)                  # 一致性惯性
    7. keep = _spatial_diverse_topk(fused, k, ...)       # 空间多样性
    8. consistency.update(keep)                          # 保存 keep
    9. _prev_recon_scores = recon_score                  # 保存 recon
    10. _prev_merged[offset:end] = img                   # 保存像素
重要：第5步和第6步的顺序不能交换。
```
## 测试方法
```
实现后可用以下方式验证：
# 1. 单元测试：单帧正确性
from pixelprune.methods import get_selector
sel = get_selector("ad_vla", method="mae")
# 模拟 3×4 merged grid, 12 个随机 patch
pv = torch.rand(12, 1536)
grid = torch.tensor([[1, 6, 8]])  # t=1, h=6, w=8 → merged h=3, w=4
idx = sel.select(pv, grid)
assert len(idx) == 1
assert len(idx[0]) <= 12
assert idx[0].ndim == 1
# 2. 首帧/第二帧状态检查
sel2 = get_selector("ad_vla")
_ = sel2.select(pv, grid)   # 首帧
assert sel2._temporal.prev_scores is not None
assert sel2._consistency.prev_keep is not None
_ = sel2.select(pv, grid)   # 第二帧
# 3. 多帧状态积累：跑 5 帧，验证 uniqueness 不为 0.5
sel3 = get_selector("ad_vla")
for _ in range(5):
    idx = sel3.select(pv, grid)
# 4. reset 后状态清空
sel3.reset()
assert sel3._temporal.prev_scores is None
assert sel3._consistency.prev_keep is None
assert sel3._prev_merged is None
# 5. 验证时序功能（故意关闭时）
sel4 = get_selector("ad_vla")
sel4._temporal.alpha = 1.0   // 关闭 score_change
sel4._temporal.beta = 0.0
sel4._consistency.gamma = 0.0  // 关闭惯性
```
## 集成到 PixelPrune 的端到端测试
```
# 设置环境变量后运行 Qwen3-VL，验证 monkey-patch 正确调用 AD-VLA
import os
os.environ["PIXELPRUNE_ENABLED"] = "true"
os.environ["PIXELPRUNE_METHOD"] = "ad_vla"
os.environ["PIXELPRUNE_VERBOSE"] = "true"
from pixelprune import apply_pixelprune
apply_pixelprune("qwen3_vl")
# 然后正常加载模型、处理视频
# 观察输出中的 DEDUP STATS 行，确认 method 显示为 ad_vla
# 对比：切换 PIXELPRUNE_METHOD=pred_2d，看保留率差异
```